'use strict'; // to require declaring variables/constants in js (on classes and modules we'll see that you don't need to do this)
console.log("Hello World");


//name = 'Donald'; there already exists in browser global variable called name which is why this is not causing an error
console.log(a);
var a = 5;
let z = 10; //let for declaring variables..........don't use var, it's old way they did it(we'll talk about why not to)
const PI = 3.14; //const for declaring constants
const name = prompt("What is your name?");
alert('Your name is ' + name);
const mytemplateString = `I am a
a multiline string`;
alert(mytemplateString);
var x = 10;